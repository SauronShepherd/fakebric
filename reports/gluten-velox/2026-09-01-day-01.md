# Gluten + Velox — Día 1 (2026-09-01)

## Alcance

Se completó el alcance contractual del Día 1 para preparar una futura ruta Spark SQL nativa con Gluten + Velox. Native permanece opt-in y desactivado por defecto; el backend por defecto sigue siendo `jvm`. No se añadió carga del plugin ni ejecución Velox, porque corresponden a días posteriores.

## Revisión previa

- HEAD inicial revisado: `c76026cb9fcb11b6cd578ab0eebf2cc4d891563e`.
- Se revisó el árbol completo de `main` y los commits recientes.
- El runtime existente fija Spark 3.5.5, Scala 2.12.18, Java 11.0.27 y Python 3.11, y solo declaraba metadata compatible con Gluten/Velox.
- Se preservó el trabajo existente de API, Power BI, notebooks, controller, Kubernetes y runtime.

## Matriz seleccionada

Perfil candidato `spark-3.5.5-gluten-1.6.0-velox-x86_64`:

- Spark 3.5.5
- Scala 2.12.18 / 2.12
- Java 17 para la variante native
- Python 3.11
- Gluten 1.6.0
- Velox commit `34338bfe5a95c895c90bb8fbbe2dfeeb04466087`, ref `dft-2026_02_06`
- arquitectura amd64 / x86_64
- Ubuntu 22.04
- artefacto `apache-gluten-1.6.0-bin-spark-3.5.tar.gz`
- tamaño verificado: `107838606` bytes
- SHA-512 oficial y verificado: `b087e962bc244d3ba4b687a953ca508182f43da962a7674d68bd9727191d6a6d124b843acefd70836cdd00647d35a45ffacd20c8989237e0d8c92c4289093a5c`
- SHA-256 calculado sobre el mismo artefacto después de verificar SHA-512: `90f9ec6ac964bcd73893c661a9133e46afc46f8b2fa9e17c647fb3939b8b6d43`

El perfil queda `candidate`, no `validated`, porque todavía no se ha construido ni probado el runtime native real.

## Archivos y decisiones

- `native-runtime-matrix.json`: matriz versionada, versiones, arquitectura, URL, tamaño y checksums SHA-512/SHA-256.
- `fakebric/native_execution.py`: `NativeExecutionConfig` v1; modos `jvm/native/auto/compare`; códigos `NATIVE_*`; diagnósticos; kill switch global `FAKEBRIC_NATIVE_EXECUTION_ENABLED`; opt-in por sesión con `enabled`; allowlist estricta de Spark; rechazo de master, secretos, endpoints, identidad Kubernetes y recursos desde sesión; la matriz exige ahora SHA-256 válido.
- `runtime-1.3.lock.json`: default `jvm`, native desactivado, estado `contract-only`, feature flag y referencia a la matriz.
- `tests/test_native_execution.py`: pruebas de contrato, matriz, flags, diagnósticos, seguridad y presencia obligatoria de SHA-256.
- `.github/workflows/native-artifact-checksum.yml`: verificación reproducible del artefacto exacto en GitHub Actions.
- `.github/workflows/ci.yml`: se corrigió el runner de tests de `pytest` a `python -m pytest` para que el paquete local sea importable en checkout limpio.

Se conserva Java 11 en el runtime JVM actual. La variante native con JDK 17 se construirá en el Día 2 para no romper el camino JVM existente. La configuración del plugin queda bajo control del runtime, no de la sesión.

## Pruebas y evidencia ejecutadas

### PASS

- Tests básicos iniciales del Día 1: **14 passed**.
- `python -m compileall -q fakebric tests` sobre el incremento inicial: **PASS**.
- `python -m json.tool native-runtime-matrix.json`: **PASS**.
- GitHub Actions `Verify Gluten native artifact`: **PASS**.
  - descargó exactamente `apache-gluten-1.6.0-bin-spark-3.5.tar.gz`;
  - verificó tamaño `107838606` bytes;
  - verificó primero el SHA-512 oficial;
  - calculó SHA-256 `90f9ec6ac964bcd73893c661a9133e46afc46f8b2fa9e17c647fb3939b8b6d43`.

### CI detectado y corregido

El primer CI del commit del Día 1 falló antes de ejecutar tests por un defecto preexistente del workflow: `pytest` se invocaba como script y el checkout no quedaba en `sys.path`, produciendo `ModuleNotFoundError: fakebric` durante la colección de todos los tests. Se cambió a `python -m pytest`, que usa correctamente el directorio del proyecto como ruta de importación. La validación del CI posterior queda como evidencia de integración del proyecto completo.

## Bloqueos y deuda

- **SHA-256 del tarball: RESUELTO.** Ya está verificado y fijado en la matriz.
- **SBOM:** pendiente del Día 2, cuando exista la variante de imagen native.
- **Docker/Minikube native:** no se presentan como validados todavía; la construcción native corresponde al Día 2 y los E2E completos a días posteriores.
- La propagación del flag por sesión al runtime corresponde al Día 8; hoy queda definido el contrato.
- Ningún operador Spark se declara soportado todavía.

## Cumplimiento y desviaciones

Cumplido: matriz, contrato versionado, modos, errores, diagnósticos, flags global/sesión, default JVM, native apagado, allowlist, rechazo de configuración insegura y artefacto verificable por SHA-256.

No se sobreafirma soporte native real: el perfil continúa `candidate` hasta disponer de runtime reproducible, plugin y smoke tests en los días siguientes.

## Siguiente paso

Día 2: construir la variante Docker reproducible native, separar artefactos JVM/native, consumir el SHA-256 ya fijado, iniciar SBOM y validar usuario no root, JDK 17, Spark 3.5.5 y arranque JVM sin plugin.
