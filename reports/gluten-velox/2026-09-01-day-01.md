# Gluten + Velox — Día 1 (2026-09-01)

## Alcance

Se completó el alcance contractual del Día 1 para preparar una futura ruta Spark SQL nativa con Gluten + Velox. Native permanece opt-in y desactivado por defecto; el backend por defecto sigue siendo `jvm`. No se añadió carga del plugin ni ejecución Velox, porque corresponden a días posteriores.

## Revisión previa

- HEAD revisado antes de trabajar: `c76026cb9fcb11b6cd578ab0eebf2cc4d891563e`.
- Se revisó el árbol completo de `main` y los commits recientes.
- El runtime existente fija Spark 3.5.5, Scala 2.12.18, Java 11.0.27 y Python 3.11, y solo declaraba metadata compatible con Gluten/Velox.
- Se preservó el trabajo existente de API, Power BI, notebooks, controller, Kubernetes y runtime.

## Matriz seleccionada

Perfil candidato `spark-3.5.5-gluten-1.6.0-velox-x86_64`:

- Spark 3.5.5
- Scala 2.12.18 / 2.12
- Java 17 para la variante native
- Python 3.11
- Gluten 1.6.0
- Velox commit `34338bfe5a95c895c90bb8fbbe2dfeeb04466087`, ref `dft-2026_02_06`
- arquitectura amd64 / x86_64
- Ubuntu 22.04
- artefacto `apache-gluten-1.6.0-bin-spark-3.5.tar.gz`
- SHA-512 oficial `b087e962bc244d3ba4b687a953ca508182f43da962a7674d68bd9727191d6a6d124b843acefd70836cdd00647d35a45ffacd20c8989237e0d8c92c4289093a5c`

El perfil queda `candidate`, no `validated`.

## Archivos y decisiones

- `native-runtime-matrix.json`: matriz versionada, versiones, arquitectura, URL, checksum y restricciones.
- `fakebric/native_execution.py`: `NativeExecutionConfig` v1; modos `jvm/native/auto/compare`; códigos `NATIVE_*`; diagnósticos; kill switch global `FAKEBRIC_NATIVE_EXECUTION_ENABLED`; opt-in por sesión con `enabled`; allowlist estricta de Spark; rechazo de master, secretos, endpoints, identidad Kubernetes y recursos desde sesión.
- `runtime-1.3.lock.json`: default `jvm`, native desactivado, estado `contract-only`, feature flag y referencia a la matriz.
- `tests/test_native_execution.py`: pruebas de contrato, matriz, flags, diagnósticos y seguridad.

Se conserva Java 11 en el runtime JVM actual. La variante native con JDK 17 se construirá en el Día 2 para no romper el camino JVM existente. La configuración del plugin queda bajo control del runtime, no de la sesión.

## Pruebas ejecutadas

### PASS

- `PYTHONPATH=. pytest -q tests/test_native_execution.py` → **14 passed**.
- `python -m compileall -q fakebric tests` sobre el incremento → **PASS**.
- `python -m json.tool native-runtime-matrix.json` → **PASS**.

Se cubrieron modo JVM por defecto, native apagado, modos requeridos, códigos de error, versiones/checksum, matriz incompleta, checksum inválido, campos arbitrarios, configuración Spark privilegiada, allowlist segura, kill switch global, doble opt-in y backend solicitado/efectivo en diagnósticos.

## Bloqueos y deuda

- **SHA-256 del tarball: BLOCKED.** No se pudo descargar el artefacto directamente desde el entorno de ejecución para calcular SHA-256. La matriz fija el SHA-512 oficial. El Día 2 debe descargar exactamente el artefacto, verificar SHA-512 y calcular/persistir SHA-256 antes de incorporarlo a una imagen.
- **SBOM:** pendiente del Día 2, cuando exista la variante de imagen native.
- **Docker/Minikube:** no ejecutados ni marcados como PASS; no forman parte de los tests básicos del Día 1.
- La propagación del flag por sesión al runtime corresponde al Día 8; hoy queda definido el contrato.
- Ningún operador Spark se declara soportado todavía.

## Cumplimiento y desviación

Cumplido: matriz, contrato versionado, modos, errores, diagnósticos, flags global/sesión, default JVM, native apagado, allowlist y rechazo de configuración insegura.

Desviación explícita: la especificación exige SHA-256 para artefactos reproducibles. Hoy se fija el SHA-512 oficial publicado, pero **el requisito SHA-256 sigue pendiente** hasta el Día 2. No se sobreafirma soporte native real.

## Siguiente paso

Día 2: construir la variante Docker reproducible native, separar artefactos JVM/native, verificar Gluten 1.6.0, calcular SHA-256, iniciar SBOM y validar usuario no root, JDK 17, Spark 3.5.5 y arranque JVM sin plugin.
