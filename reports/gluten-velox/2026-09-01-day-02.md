# Gluten + Velox — Día 2 adelantado (2026-09-01)

## Alcance

Se adelantó el alcance previsto para el Día 2 sin activar ejecución native. El resultado es una base reproducible separada para JVM y para la futura ruta Gluten + Velox, manteniendo `jvm` como backend efectivo por defecto.

## Estado revisado antes de trabajar

- HEAD inicial del Día 2: `176a6863411f56958a2b5b1e7d8fe2d2ed25b3fc`.
- El CI posterior al Día 1 confirmó tests y checks estáticos.
- Se detectó y corrigió un fallo preexistente: el CI construía el supuesto runtime desde el `Dockerfile` raíz de la API en lugar de `runtime/Dockerfile`, por lo que el notebook smoke no encontraba `/opt/fakebric/execute_notebook.py`.

## Bases y versiones fijadas

GitHub Actions resolvió para `linux/amd64`:

- Python 3.11 slim: `python@sha256:d1e9ca7c4e78d1e8ecadb5d44bfc8e956e7a65b659a9950f569f243d72b326d0`
- JVM Java 11: `eclipse-temurin@sha256:c55276a409ff48855ce7011fda07da527c41d57cc63b3381533fb7fea3ae027a`
- Native candidate Java 17.0.20+8: `eclipse-temurin@sha256:e17d77fb030dd4b642dc078d048a5fb9efcb3676ee20305d905949105a6ccd5a`

El runtime mantiene Spark 3.5.5, Scala 2.12.18 y Python 3.11. La variante native usa Java 17 sin modificar la ruta JVM por defecto.

## Artefactos del Día 2

- `runtime/Dockerfile`: runtime JVM separado, bases fijadas por digest, `tini`, UID 10001 y native desactivado.
- `runtime/native.Dockerfile`: variante separada Java 17 + Spark 3.5.5 + Gluten 1.6.0, todavía sin inicializar el plugin.
- `runtime/runtime_versions.py`: evidencia de Python, Java, Spark, arquitectura, UID/GID y presencia/estado native.
- `runtime/native-artifact-checksums.txt`: SHA-256 y SHA-512 del tarball de Gluten.
- `runtime/native-sbom.spdx.json`: SBOM preliminar SPDX 2.3.
- `runtime-1.3.lock.json`: referencias a imágenes base, Dockerfiles, SBOM y checksums; estado `runtime-candidate`.
- `tests/test_native_runtime_assets.py`: pruebas estáticas de separación, pins, hashes, native-off y SBOM.
- `.github/workflows/runtime-base-lock.yml`: evidencia de resolución de digests.
- `tools/validate_k8s_manifests.py`: validación realmente offline de sintaxis YAML y estructura mínima de objetos Kubernetes, sin fingir validación OpenAPI.

## Reproducibilidad de Gluten

`runtime/native.Dockerfile` descarga exactamente `apache-gluten-1.6.0-bin-spark-3.5.tar.gz` y antes de extraer exige:

1. tamaño `107838606` bytes;
2. SHA-512 oficial `b087e962bc244d3ba4b687a953ca508182f43da962a7674d68bd9727191d6a6d124b843acefd70836cdd00647d35a45ffacd20c8989237e0d8c92c4289093a5c`;
3. SHA-256 `90f9ec6ac964bcd73893c661a9133e46afc46f8b2fa9e17c647fb3939b8b6d43`.

No se permite instalación arbitraria de plugins durante una sesión.

## Seguridad y límites

- `FAKEBRIC_NATIVE_EXECUTION_ENABLED=false` en JVM y native-capable.
- La presencia de Gluten no activa `spark.plugins`.
- Usuario final UID 10001, no root.
- `/workspace` y `/tmp/spark` deben ser escribibles por el usuario de runtime.
- `tini` debe estar disponible en ambas imágenes.
- El smoke de la imagen native-capable aplica `--memory=3g --cpus=2`.
- La imagen native continúa como candidata y sin digest de producción.

## Evidencia obtenida

La ejecución final de CI del commit `16fd1dd37d0d2f6d94cb3a990afa7048af1759e3` terminó completamente en verde. Pasaron:

- suite Python completa con cobertura >=75% y checks estáticos;
- builds de API y controller;
- build real de `runtime/Dockerfile`;
- Python 3.11, Java 11, Spark 3.5.5, `x86_64`, UID 10001 y native desactivado en JVM;
- notebook Spark smoke en JVM;
- build de `runtime/native.Dockerfile`, verificando tamaño + SHA-512 + SHA-256 durante el build;
- Python 3.11, Java 17.0.20, Spark 3.5.5, `x86_64`, UID 10001, Gluten 1.6.0 y pin exacto de Velox;
- presencia de contenido extraído de Gluten, SBOM y checksums dentro de la imagen native;
- Spark JVM dentro de la imagen native-capable con `spark.plugins` vacío y native desactivado;
- validación YAML offline de los manifiestos Kubernetes;
- consistencia del pin de la imagen controller.

Los intentos anteriores de usar `kubectl --dry-run=client` sin API server no se contaron como validación offline porque ese comando todavía hace descubrimiento para determinados recursos. La comprobación final usa parsing YAML + estructura mínima con Python. La validación Kubernetes/OpenAPI estricta permanece reservada al gate con Minikube.

## Comprobaciones de cierre

La comprobación de inventario native no presupone una extensión concreta como `*.so*`: verifica que `/opt/gluten` contiene archivos extraídos y que `FAKEBRIC_RUNTIME_LOCK` identifica Gluten 1.6.0, el commit exacto de Velox y los checksums fijados. También se verifican `x86_64`, `tini`, directorios escribibles y native desactivado.

## Estado final

**Día 2: PASS.** El runtime JVM y la variante native-capable reproducible están construidos y validados por CI. Native permanece desactivado y `spark.plugins` vacío. No se declara todavía ejecución Velox real ni operadores soportados; la inicialización controlada de Gluten/Velox corresponde al Día 3.