# Gluten + Velox — Día 2 adelantado (2026-09-01)

## Alcance

Se adelantó el alcance previsto para el Día 2 sin activar ejecución native. El objetivo es disponer de una base reproducible separada para JVM y para la futura ruta Gluten + Velox, manteniendo `jvm` como backend efectivo por defecto.

## Estado revisado antes de trabajar

- HEAD inicial del Día 2: `176a6863411f56958a2b5b1e7d8fe2d2ed25b3fc`.
- El CI posterior al Día 1 confirmó que tests y checks estáticos pasan.
- Se detectó un fallo preexistente en el job de imágenes: el workflow construía la imagen runtime con el `Dockerfile` raíz de la API en lugar de `runtime/Dockerfile`, por lo que `/opt/fakebric/execute_notebook.py` no existía y el smoke Spark fallaba. La corrección queda incluida en este Día 2.

## Bases y versiones fijadas

GitHub Actions resolvió los siguientes digests para `linux/amd64`:

- Python 3.11 slim: `python@sha256:d1e9ca7c4e78d1e8ecadb5d44bfc8e956e7a65b659a9950f569f243d72b326d0`
- JVM Java 11: `eclipse-temurin@sha256:c55276a409ff48855ce7011fda07da527c41d57cc63b3381533fb7fea3ae027a`
- Native candidate Java 17.0.20+8: `eclipse-temurin@sha256:e17d77fb030dd4b642dc078d048a5fb9efcb3676ee20305d905949105a6ccd5a`

El runtime mantiene Spark 3.5.5, Scala 2.12.18 y Python 3.11. La variante native usa Java 17 sin modificar la imagen JVM existente.

## Artefactos añadidos/cambiados

- `runtime/Dockerfile`: base JVM fijada por digest, native explícitamente desactivado y herramienta de inventario de versiones.
- `runtime/native.Dockerfile`: variante separada Java 17 + Spark 3.5.5 + Gluten 1.6.0.
- `runtime/runtime_versions.py`: evidencia ejecutable de Python, Java, Spark, arquitectura, UID/GID y presencia/estado native.
- `runtime/native-artifact-checksums.txt`: SHA-256 y SHA-512 del tarball de Gluten.
- `runtime/native-sbom.spdx.json`: SBOM preliminar SPDX 2.3 de componentes principales.
- `runtime-1.3.lock.json`: referencias a Dockerfiles, SBOM, checksums, base-image digests y estado `runtime-candidate`.
- `tests/test_native_runtime_assets.py`: pruebas estáticas de separación, pins, hashes, default native off y SBOM.
- `.github/workflows/ci.yml`: construye el runtime JVM correcto, repara su smoke, construye la variante native y valida ambos perfiles.
- `.github/workflows/runtime-base-lock.yml`: evidencia reproducible de los digests de base usados en el Día 2.

## Reproducibilidad del artefacto Gluten

`runtime/native.Dockerfile` descarga exactamente `apache-gluten-1.6.0-bin-spark-3.5.tar.gz` y antes de extraerlo exige:

1. tamaño `107838606` bytes;
2. SHA-512 oficial `b087e962bc244d3ba4b687a953ca508182f43da962a7674d68bd9727191d6a6d124b843acefd70836cdd00647d35a45ffacd20c8989237e0d8c92c4289093a5c`;
3. SHA-256 verificado `90f9ec6ac964bcd73893c661a9133e46afc46f8b2fa9e17c647fb3939b8b6d43`.

No se instala ningún plugin arbitrario durante una sesión.

## Contrato de seguridad conservado

- Native sigue desactivado por defecto: `FAKEBRIC_NATIVE_EXECUTION_ENABLED=false` en ambas imágenes.
- La existencia del runtime Gluten no activa `spark.plugins`.
- Usuario final `10001`, no root.
- `/workspace` y `/tmp/spark` son los únicos directorios de trabajo creados para la sesión en esta fase.
- La imagen native sigue marcada como candidata y sin digest de producción.

## Validaciones preparadas en CI

El CI debe confirmar antes de cerrar el Día 2:

- suite Python completa + cobertura >=75%;
- compileall y SBOM JSON válido;
- build de API/controller sin regresiones;
- build del runtime JVM usando `runtime/Dockerfile`;
- Python 3.11, Java 11, Spark 3.5.5, UID 10001 y native ausente/desactivado en JVM;
- notebook Spark smoke exitoso en JVM;
- build de `runtime/native.Dockerfile` con hashes/tamaño verificados durante build;
- Python 3.11, Java 17.0.20, Spark 3.5.5, UID 10001, Gluten 1.6.0 y Velox pin en native;
- SBOM/checksums presentes dentro de la imagen native;
- arranque Spark JVM dentro de la imagen native con `spark.plugins` vacío y native desactivado;
- validación de manifiestos Kubernetes y consistencia de la imagen controller.

## Límites intencionados

No se configura ni inicializa todavía el plugin Gluten. Tampoco se declara ejecución Velox real ni operadores soportados. Eso corresponde al Día 3 y posteriores. El Día 2 solo entrega la base reproducible y verificable necesaria para hacerlo de forma segura.

## Estado

Pendiente de la ejecución de CI asociada al commit final del Día 2. Solo se marcarán como PASS las comprobaciones que GitHub Actions ejecute con éxito; cualquier fallo se corregirá o se registrará como BLOCKED sin sobreafirmar resultados.
