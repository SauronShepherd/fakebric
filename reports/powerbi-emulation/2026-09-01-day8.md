# Power BI emulation — informe Día 8 (ejecución anticipada)

- **Fecha de ejecución:** 2026-09-01 (Europe/Madrid)
- **Día del plan ejecutado:** Día 8 — informes, páginas y visuales (planificado originalmente para 2026-09-08)
- **Rama:** `codex/powerbi-emulation`
- **Commit funcional:** `21019fc543091270ea6f59ed5e9d3aa41cef3cb5`

## Alcance

Se amplía `Report` desde el contrato base de Día 1 a páginas ordenadas con tamaño/orientación, queries, filtros report/page/visual, tema y formato. El catálogo controlado incluye card, table, matrix, bar, column, line, pie, donut, scatter y slicer; cada tipo tiene un schema de propiedades discriminado y validación de geometría/referencias.

`ReportRuntime` produce un primer runtime web pasivo y seguro con estados loading/empty/error/ready, payload serializado y markup HTML escapado con `role=group` y `aria-label`. No incorpora interacciones, cross-filtering, drill ni exportación del Día 9.

Se añade CRUD standalone `/api/v1/reports` con ETag/revision y render mínimo. La integración persistida con el control plane se mantiene para Día 10.

## Evidencia local

La batería focalizada de Día 8 ejecutó `11 passed`; `compileall` y `git diff --check` también pasaron. Cubre contratos de los diez visuales, fixture renderizable de ocho tipos, geometría inválida, query/filter references, páginas vacías, estados runtime, escape HTML, CRUD/ETag y schema drift básico.
