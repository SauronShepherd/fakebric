# Power BI emulation — informe Día 2 (ejecución anticipada)

- **Fecha de ejecución:** 2026-09-01 (Europe/Madrid)
- **Día del plan ejecutado:** Día 2 — tablas, columnas y fuentes (planificado originalmente para 2026-09-02)
- **Rama:** `codex/powerbi-emulation`
- **Base revisada:** `3a64a6b08597ee98d07c760514e2e117096638b5`
- **Commit funcional:** `c64ee26b9a587dc4bd853476340856e18c9b678a`

## Revisión de días anteriores

Se revisaron los contratos del Día 1 (`SemanticModel`, `Report`, estados, ETag/repositorio y feature flag). Los tests focalizados de Día 1 se incluyeron de nuevo en la ejecución conjunta del incremento del Día 2 y no aparecieron regresiones.

## Alcance implementado

- Entidades tipadas `Table`, `Column`, `Measure` y `DataSource`.
- Tipos `string`, `integer`, `decimal`, `boolean`, `date`, `datetime` y `binary`.
- Fuentes CSV, JSONL, Parquet y tablas Fakebrick registradas localmente.
- Inferencia de tipos como sugerencia revisable (`suggestedType`, `nullable`, `confidence`).
- Conversión explícita de tipos y rechazo de conversiones inválidas.
- Validación de nombres duplicados y de columnas físicas inexistentes.
- Perfil de dataset con filas, nulls, min, max y cardinalidad.
- Protección contra path traversal y fuentes de fichero fuera del workspace.
- Rechazo de credenciales inline en opciones de fuente; soporte de referencia `credentialRef`.
- Fixtures deterministas de CSV/JSONL, inferencia y estadísticas.
- JSON Schema de `SemanticModel` actualizado para reflejar las entidades del Día 2.

## Pruebas y evidencia

Ejecución focalizada:

```text
python -m pytest -q tests/test_semantic_model.py tests/test_reports.py tests/test_semantic_model_sources.py
24 passed, 1 skipped
```

El único `skip` es el round-trip Parquet porque el runtime de verificación utilizado no tenía `pyarrow` instalado. `pyarrow==25.0.1` se añadió a `requirements.txt`; el test no se marca como PASS hasta ejecutarse en un entorno que instale las dependencias del repositorio.

También se ejecutó:

```text
python -m compileall -q fakebric tests
PASS
```

La verificación focalizada confirmó CSV, JSONL, tablas Fakebrick locales, dataset vacío, valores nulos, inferencia, conversiones para los siete tipos, conversiones inválidas, duplicados, columnas inexistentes, credentialRef y path traversal.

## Defectos encontrados/corregidos durante la implementación

- Se ajustó la inferencia para que `1`/`0` se interpreten como enteros en vez de booleanos durante inferencia heurística; la conversión explícita booleana sigue aceptándolos.
- Se ajustó `load_table` para devolver únicamente las columnas semánticas declaradas, evitando filtrar accidentalmente columnas físicas no declaradas.
- Se añadió rechazo explícito de opciones con nombres de credencial/secreto para respetar la regla de no embebido de credenciales.

## Estado de compatibilidad

`SemanticModel` pasa de contrato base a `partial` con tablas, columnas, medidas y fuentes. CSV/JSONL y tablas Fakebrick tienen evidencia focalizada de lectura. Parquet está implementado mediante `pyarrow`, pero su evidencia de ejecución queda pendiente de CI/entorno con dicha dependencia. Relaciones y validación del grafo siguen fuera de alcance hasta el Día 3. DAX, consultas y visuales siguen `unsupported` según el calendario.

## Limitaciones y bloqueos

- No se ejecutó la suite histórica completa sobre un checkout completo del repositorio en este runtime; por tanto no se declara como PASS.
- `git diff --check` sobre checkout local completo no está disponible en esta vía de ejecución; no se declara como PASS.
- El test Parquet queda pendiente de ejecución con `pyarrow` instalado.
- No se adelanta trabajo de relaciones (Día 3) ni DAX (Días 4–6).

## Siguiente paso

Día 3: relaciones 1:1/1:N/N:M, dirección de filtro, activas/inactivas, PK/FK/date table, detección de ciclos/ambigüedad/duplicados, grafo de dependencias y endpoint de validación con diagnósticos estructurados.
