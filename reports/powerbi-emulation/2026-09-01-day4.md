# Power BI emulation — informe Día 4 (ejecución anticipada)

- **Fecha de ejecución:** 2026-09-01 (Europe/Madrid)
- **Día del plan ejecutado:** Día 4 — lexer, parser y AST DAX (planificado originalmente para 2026-09-04)
- **Rama:** `codex/powerbi-emulation`
- **Base revisada:** `ed0f4d15d271243d62df20bfbf645aef9fcc6f69`
- **Commit funcional:** `19a41ff609600cbeba36a2639984fce6acf1936d`

## Alcance implementado

- Lexer para identificadores, referencias calificadas, strings, números, literales `dt"..."`, operadores y paréntesis.
- Parser con precedencia y asociatividad controladas.
- AST inmutable y serializable a estructuras JSON.
- Diagnósticos con código, línea, columna, token y mensaje.
- Límites de longitud, tokens, profundidad y número de nodos AST.
- Rechazo determinista de funciones no publicadas, referencias ambiguas, comparaciones encadenadas y sintaxis no DAX.
- Catálogo de funciones por niveles 1–3 para aceptación sintáctica.
- Golden tests del AST y pruebas de seguridad para impedir sintaxis de Python/SQL/URL arbitraria.

## Verificación

En el entorno reconstruido del incremento se ejecutó una batería focalizada que incluye parser/lexer/AST, golden/security tests, integración semántica experimental local y regresión del validador del Día 3:

```text
32 passed in 0.28s
python -m compileall -q fakebric tests   # PASS
git diff --cached --check               # PASS
```

El commit remoto funcional contiene el parser y sus tests propios. La integración profunda del evaluador y la semántica de ejecución se mantiene fuera de alcance hasta los Días 5–6.

## Defectos/correcciones durante la ejecución

- Se sustituyó una sintaxis de fecha provisional por el literal DAX `dt"..."` antes de publicar.
- Se mantuvo la separación estricta entre **parsear** y **evaluar** una función para no adelantar el Día 5.

## Estado

DAX pasa de `unsupported` a `partial`: el lenguaje controlado puede tokenizarse y parsearse de forma segura, pero todavía no ejecuta medidas ni agregaciones. La ejecución de Nivel 1 corresponde al Día 5.
