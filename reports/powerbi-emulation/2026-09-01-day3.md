# Power BI emulation — informe Día 3 (ejecución anticipada)

- **Fecha de ejecución:** 2026-09-01 (Europe/Madrid)
- **Día del plan ejecutado:** Día 3 — relaciones y validador de modelos (planificado originalmente para 2026-09-03)
- **Rama:** `codex/powerbi-emulation`
- **Base revisada:** `d07aee472826d3fa9b5b29aea0118e087870d559`
- **Commit funcional:** `6815d3b423c8c5cf2a2bbc69fb8d81bfd06f8164`

## Revisión de días anteriores

Se revisaron los contratos y fuentes construidos en los Días 1–2. La regresión focalizada confirma round-trip/versionado del Día 1 y compatibilidad de `Table`, `Column`, `Measure`, `DataSource`, tipos y `credentialRef` del Día 2.

## Alcance implementado

- Relaciones `one-to-one`, `one-to-many` y `many-to-many`.
- Dirección de filtro `single` y `both`.
- Relaciones activas e inactivas.
- Marcado `isPrimaryKey`, `isDateTable` y `dateColumn`.
- Dependencias explícitas de tabla/columna/medida mediante `dependsOn`.
- Grafo de dependencias con detección de destinos inexistentes y ciclos.
- Grafo de propagación de filtros.
- Detección de relaciones duplicadas, tipos incompatibles, columnas/tablas inexistentes, ciclos y caminos ambiguos.
- Diagnósticos con `severity`, `code`, `location`, `message` y `suggestedFix`.
- Endpoint `POST /api/v1/models/{id}/validate` en el servicio/router semántico.

No se analiza todavía el texto DAX para derivar dependencias: hacerlo antes del lexer/parser del Día 4 violaría el orden del plan. Las dependencias de este incremento son contractuales y explícitas.

## Pruebas y evidencia

```text
PYTHONPATH=. pytest -q
12 passed

python -m compileall -q fakebric tests
PASS

git diff --cached --check
PASS
```

Se cubren propagación 1:N, relación inactiva, 1:1, N:M, `both`, incompatibilidad de tipos, relación duplicada, columna inexistente, ciclos/caminos ambiguos, dependencia inexistente, tabla de fechas, contrato HTTP y mismatch de model id.

## Defecto encontrado/corregido

Durante la verificación el router se incluyó en la app FastAPI antes de registrar la ruta `/validate`, lo que producía 404. Se corrigió montando el router después de declarar el handler y se repitió la suite completa focalizada: 12 PASS.

## Estado de compatibilidad

El modelo semántico avanza a `partial` con grafo de relaciones y validación estructurada. El endpoint está disponible como servicio/router semántico aislado; la integración con el control plane persistido se mantiene para el Día 10 conforme al plan. DAX continúa `unsupported` hasta los Días 4–6.

## Limitaciones

- No se ejecutó la suite histórica completa desde checkout remoto porque el runtime no resuelve `github.com` por DNS; los cambios se escriben mediante el conector GitHub autorizado.
- `pyarrow` no está instalado en este runtime, por lo que no se repitió aquí el round-trip Parquet del Día 2.
- No se adelanta lexer/parser DAX ni query engine.

## Siguiente paso

Día 4: lexer, parser y AST DAX controlado, precedencia, diagnósticos línea/columna/token, límites de complejidad, catálogo de funciones soportadas y golden AST tests.
