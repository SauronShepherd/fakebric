# Power BI emulation — informe Día 6 (ejecución anticipada)

- **Fecha de ejecución:** 2026-09-01 (Europe/Madrid)
- **Día del plan ejecutado:** Día 6 — filtros, relaciones y CALCULATE (planificado originalmente para 2026-09-06)
- **Rama:** `codex/powerbi-emulation`
- **Base revisada:** `ff8aca0974f793126068236cd1104f1e9a183dd2`
- **Commit funcional:** `dcb4e7f9ea8c5c40840f9aaa5bf28f9f32d0478e`

## Revisión y alcance

La rama se comparó contra la base del Día 5 antes de escribir y estaba `identical`, sin commits paralelos. Se implementó el alcance del Día 6: funciones N2, transición de contexto, precedencia de filtros, propagación por relaciones y rechazo de grafos ambiguos. No se adelantó el planner/caché del Día 7.

La semántica CALCULATE usada es reemplazo de filtros sobre la misma columna salvo `KEEPFILTERS`, que intersecta. `ISFILTERED` refleja filtro directo; `HASONEVALUE`/`SELECTEDVALUE` observan el conjunto visible tras cross-filter.

## Verificación local

Durante el desarrollo, la reconstrucción focalizada completa alcanzó `51 passed` con parser/golden/security, N1 y escenarios N2; `compileall` y `git diff --check` pasaron en esa reconstrucción. Tras compactar la implementación para la publicación remota se volvió a ejecutar un smoke integrado que cubre propagación, CALCULATE, KEEPFILTERS, FILTER, modificadores y medidas, además de `compileall`.

Esto no sustituye la suite histórica completa del repositorio ni E2E/Minikube. El estado de CI del commit remoto se consulta aparte y no se marca como verde sin evidencia.

## Límites explícitos

No se implementaron planner scan/filter/join/aggregate/project, DuckDB/Arrow, paginación, timeout/cancelación, caché ni métricas de consulta; todo ello corresponde al Día 7. Tampoco se pretende compatibilidad DAX completa fuera del catálogo controlado.
